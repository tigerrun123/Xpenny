[package]
name = "openclaw_invocation"
version = "0.1.0"
description = "Solana invocation layer for AWS-hosted OpenClaw agents"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "openclaw_invocation"

[features]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
cpi = ["no-entrypoint"]
default = []

[dependencies]
anchor-lang = "0.30.1"
